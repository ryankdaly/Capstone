# ============================================================
# HPEMA — Python dependencies
# Tested with Python 3.12
# ============================================================

# -----------------------------------------------------------
# Core backend (FastAPI + config)
# -----------------------------------------------------------
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0          # v2 required — v1 API is incompatible
pyyaml>=6.0

# -----------------------------------------------------------
# LLM client
# Talks to any OpenAI-compatible endpoint: vLLM, OpenAI,
# Groq, ARC, etc. openai>=1.0 required for AsyncOpenAI.
# -----------------------------------------------------------
openai>=1.0
httpx>=0.27.0

# -----------------------------------------------------------
# CLI
# -----------------------------------------------------------
typer>=0.12.0
rich>=13.0
prompt_toolkit>=3.0   # autocomplete, history, ghost-text suggestions

# -----------------------------------------------------------
# RAG — vector search for the Policy agent
# ChromaDB uses its own ONNX embedding backend by default
# (no separate sentence-transformers needed).
# numpy<2.0 required for ChromaDB 0.4–0.6 ONNX compatibility.
# -----------------------------------------------------------
chromadb>=0.4.0,<0.7.0
numpy<2.0

# -----------------------------------------------------------
# Testing
# pytest-asyncio needed for all async agent/pipeline tests.
# anyio needed by pytest-asyncio for async test collection.
# -----------------------------------------------------------
pytest>=8.0
pytest-asyncio>=0.23.0
anyio>=4.0

# -----------------------------------------------------------
# Dashboard (optional — only needed for: streamlit run dashboard/app.py)
# -----------------------------------------------------------
streamlit>=1.30.0
